[project]
name = "week-01-openai-agents"
version = "0.1.0"
description = "Week 1: planner + parallel research agents with OpenAI structured outputs."
requires-python = ">=3.11"
dependencies = [
    "openai>=1.76.0",
    "pydantic>=2.7.0",
    "python-dotenv>=1.0.0",
    "playwright>=1.49.0",
]
