[project]
name = "week-02-langchain-agents"
version = "0.1.0"
description = "Week 2: planner and parallel research agents with LangChain structured outputs."
requires-python = ">=3.11"
dependencies = [
    "langchain>=0.3.0",
    "langchain-openai>=0.3.0",
    "pydantic>=2.7.0",
    "python-dotenv>=1.0.0",
    "playwright>=1.49.0",
]
