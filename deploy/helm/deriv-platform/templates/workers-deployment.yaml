{{- if .Values.workers.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "deriv-platform.fullname" . }}-planner
  namespace: {{ .Values.namespace.name }}
  labels:
    app.kubernetes.io/component: planner
    {{- include "deriv-platform.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.workers.planner.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/component: planner
      {{- include "deriv-platform.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app.kubernetes.io/component: planner
        {{- include "deriv-platform.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: planner
          image: {{ include "deriv-platform.image" (dict "Values" .Values "repository" .Values.workers.repository) }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["uv", "run", "python", "-m", "planner.main"]
          {{- include "deriv-platform.secretEnvFrom" . | nindent 10 }}
          env:
            - name: POSTGRES_DATABASE_URL
              value: {{ include "deriv-platform.postgresql.url" . | quote }}
            - name: KAFKA_BOOTSTRAP
              value: {{ include "deriv-platform.kafka.bootstrap" . | quote }}
            {{- range $key, $val := .Values.workers.env }}
            - name: {{ $key }}
              value: {{ $val | quote }}
            {{- end }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "deriv-platform.fullname" . }}-executor
  namespace: {{ .Values.namespace.name }}
  labels:
    app.kubernetes.io/component: executor
    {{- include "deriv-platform.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.workers.executor.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/component: executor
      {{- include "deriv-platform.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app.kubernetes.io/component: executor
        {{- include "deriv-platform.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: executor
          image: {{ include "deriv-platform.image" (dict "Values" .Values "repository" .Values.workers.repository) }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["uv", "run", "python", "-m", "executor.main"]
          {{- include "deriv-platform.secretEnvFrom" . | nindent 10 }}
          env:
            - name: POSTGRES_DATABASE_URL
              value: {{ include "deriv-platform.postgresql.url" . | quote }}
            - name: KAFKA_BOOTSTRAP
              value: {{ include "deriv-platform.kafka.bootstrap" . | quote }}
            {{- range $key, $val := .Values.workers.env }}
            - name: {{ $key }}
              value: {{ $val | quote }}
            {{- end }}
{{- end }}
