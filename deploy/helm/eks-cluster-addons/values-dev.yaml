# Dev — set from Jenkins global env vars (terraform output; see jenkins/global-env.example).

aws-load-balancer-controller:
  clusterName: deriv-ai-bot-dev
  region: us-west-1
  vpcId: ""
  serviceAccount:
    annotations:
      eks.amazonaws.com/role-arn: ""

external-dns:
  txtOwnerId: deriv-dev
  domainFilters:
    - testenvlab.shop
  serviceAccount:
    annotations:
      eks.amazonaws.com/role-arn: ""
